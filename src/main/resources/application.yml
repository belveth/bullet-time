server:
  port: 3000

spring:
  datasource:
    bullettime:
      writer:
        driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
        jdbc-url: jdbc:log4jdbc:mysql://localhost:3306/bullettime?useUnicode=true&serverTimezone=UTC
        username: root
        password:
      reader:
        driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
        jdbc-url: jdbc:log4jdbc:mysql://localhost:3306/bullettime?useUnicode=true&serverTimezone=UTC
        username: root
        password:
